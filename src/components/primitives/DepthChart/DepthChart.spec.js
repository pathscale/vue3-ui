import DepthChart from "./DepthChart.vue";

let wrapper;

describe("DepthChart", () => {
	beforeEach(() => {
		wrapper = DepthChart;
	});

	it("initialize", () => {
		expect(wrapper.exist).toBe(wrapper.exist);
	});
});
